{
  "name": "extensy-docs",
  "version": "1.0.0",
  "description": "Official documentation for Extensy built with Mintlify.",
  "scripts": {
    "dev": "mintlify dev",
    "build": "mintlify export --output export.zip && mkdir -p out && cd out && unzip -o ../export.zip && cd ..",
    "start": "mintlify dev"
  },
  "dependencies": {
    "mintlify": "latest"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "typescript": "^5.0.0"
  }
}
